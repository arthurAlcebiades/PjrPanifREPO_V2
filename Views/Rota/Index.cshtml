@model IEnumerable<RotaModel>
@{
    ViewBag.Subtitulo = "Listagem de Rotas";
}

<header class="d-flex justify-content-between align-items-center">
    <div>
        <h1 class="text-primary">@ViewBag.Subtitulo</h1>
    </div>
    <div>
        <a asp-action="Cadastrar" class="btn btn-primary">Nova Rota</a>
    </div>
</header>
<hr>

<partial name="_Mensagem"></partial>

@if (Model.Count() > 0)
{
    <table class="table table-striped tabela-produtos">
        <thead>
            <tr>
                <th>Nome da Rota</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var rota in Model)
            {
                <tr>
                    <td>@rota.ApelidoRota</td>
                    <td>
                        <a asp-action="Cadastrar" asp-route-id="@rota.IdRota" class="btn btn-sm btn-secondary"><i class="bi-pencil" title="Alterar"></i></a>
                        <a asp-action="Excluir" asp-route-id="@rota.IdRota" class="btn btn-sm btn-danger"><i class="bi-trash" title="Excluir"></i></a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>Não há nenhuma rota cadastrada.</p>
}